# Base path the frontend calls for the API. Left as "/api" so the Vite dev
# proxy (see vite.config.js) and a same-origin production deploy both work
# with zero changes. Point it at an absolute URL only if the API is served
# from a different origin than the built frontend.
VITE_API_BASE_URL=/api
